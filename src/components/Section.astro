---
interface Props {
	title: string;
	id?: string;
}

const { title, id } = Astro.props;
---

<section id={id} class="mt-16 first:mt-4">
	<h2 class="mb-5 text-xl font-bold tracking-tight">{title}</h2>
	<slot />
</section>
